
## 嵌入式端代码：

- 嵌入式代码在 /Users/shujun/mcu/xiaozhi-esp32
- 已经配置好了 board，我们使用 ESP32S3 , board 选择  main/boards/bread-compact-wifi


## 编译、烧录、日志


请使用 tmux 来查看日志，这样我也可以看到

```shell
# 初始化idf环境变量
espenv 

# 查看版本
idf.py --version

# 编译
idf.py build

# 打开串口日志
idf.py monitor


# 组合命令
idf.py build flash monitor

通过 ctrl + ] 退出当前日志
```

## 服务器端代码

- 服务器端代码路径  /Users/shujun/work/aiot-monorepo， 负责对接 xiaozhi 设备的服务是 xzserver。

## 服务器端部署

- 在 名字为 hunan2 的 tmux 窗口中，我们已经登录到我们的线上服务器。
- 相关配置参考  /root/nuc2-deploy/aiot/env.hunan2
- nginx 已经反向代理了 xzserver 的 http 地址，https://build.uling.top:60443/xiaozhi/ota/

## 设备端

- xiaozhi 硬件设备已经接入到当前电脑，可以直接烧录，以及看到串口日志。
- 设备已经连接到当前局域网，SSID 是  BS2024
- 设备连接本机运行的 xzserver 已经通过语音交互的测试。

## 任务

修改代码和配置，最终将 xiaozhi 硬件连接到我们线上服务器部署的 aiot ，并确保语音交互正常。

- xiaozhi 硬件连接原来的 xiaozhi 服务器是正常的。
- xzserver 实现了 xiaozhi 的协议，包括 mqtt + udp，和 websocket
- xzserver 可用 xzclient 调试确认功能正常：包括语音交互正常，以及接入的流程。

接入流程一步步验证，包括用到的 http 接口和相关配置：
```
//   - POST/GET /xiaozhi/ota/            → 引导接口 [A]
//   - POST     /xiaozhi/ota/activate    → 激活接口 [B]
```

